package org.example.config;import org.example.interfaces.SagaSteps;import org.springframework.stereotype.Service;import java.util.ArrayList;import java.util.List;@Servicepublic class SagaOrchestrator {    private  List<SagaSteps> steps = new ArrayList<>();    public  void addStep(SagaSteps step) {        steps.add(step);    }    public  void execute() {        try {            for (SagaSteps step : steps) {                step.execute();            }        } catch (Exception e) {            compensate();            throw new RuntimeException("Saga execution failed", e);        }    }//    private static void compensate() {//        for (int i = steps.size() - 1; i >= 0; i--) {//            steps.get(i).compensate();//        }//    }    private void compensate() {        for (int i = 0 ; i <steps.size() ; i++) {            steps.get(i).compensate();        }    }}