package org.example.service.orchestorCalls;import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;import org.example.interfaces.SagaSteps;import org.example.model.Author;import org.example.repository.AuthorDao;public class CreateAuthor implements SagaSteps {    private final AuthorDao authorDao;    private final Author author;    public CreateAuthor(AuthorDao authorDao, Author author) {        this.authorDao = authorDao;        this.author = author;    }    @Override    public void execute() {        authorDao.insert(author);    }    @Override    public void compensate() {        Author author1 = authorDao.selectOne(new QueryWrapper<Author>().eq("rollbackId",author.getRollbackId()));//        authorDao.deleteById(author.getId());        if (author1 !=null)            authorDao.deleteById(author1.getId());    }}