grpc:
  server:
    port: 6000
  client:
    grpc-userService:
      address: static://localhost:9405
      negotiation-type: plaintext
      #if it is not secure TLS call

server:
  port: 9303

spring:
  application:
    name: accountService
  datasource:
    url: jdbc:mysql://localhost:3306/seata_capital?allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root