grpc:
  server:
    port: 6000
  client:
    grpc-userService:
      address: static://localhost:9405
      negotiation-type: plaintext
      #if it is not secure TLS call

server:
  port: 9304

spring:
  application:
    name: capitalService
  datasource:
    url: jdbc:mysql://localhost:3306/seata_capital?allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root


rocketmq:
  name-server: 127.0.0.1:9876
  producer:
    group: capitalService-producer-group
  consumer:
    group: capitalService-consumer-group